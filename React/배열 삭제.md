# 배열 삭제

```js
const numbers = [1, 2, 3, 4, 5, 6];
const oddNumbers = numbers.filter((number, index, arr) => {
  console.log(number, index, arr);
  return number % 2;
});
console.log(oddNumbers);
```
